
# bug#31609 Not all RBR replica errors reported as errors
# bug#31552 Replication breaks when deleting rows from out-of-sync table
#           without PK

# The default for replica-exec-mode option and server
# variable replica_exec_mode  is 'STRICT'.
# When 'STRICT' mode is set, the replica SQL thread will stop whenever
# the row to change is not found. In 'IDEMPOTENT' mode, the SQL thread
# will continue running and apply the row - replace if it's Write_rows event -
# or skip to the next event.

# the previous part of the tests was with IDEMPOTENT replica's mode.


#
# Other than above idempotent errors dealing with foreign keys constraint
#
connection replica;

set @old_replica_exec_mode= @@global.replica_exec_mode;
set @@global.replica_exec_mode= IDEMPOTENT;

connection primary;

create table ti1 (b int primary key) engine = innodb;
create table ti2 (a int primary key, b int, foreign key (b) references ti1(b))
     engine = innodb;
set foreign_key_checks=1 /* ensure the check */;

insert into ti1 values (1),(2),(3);
insert into ti2 set a=2, b=2;

--source include/sync_replica_sql_with_primary.inc

#connection replica;
select * from ti1 order by b /* must be (1),(2),(3) */;
insert into ti2 set a=1, b=1;
select * from ti2 order by b /* must be (1,1) (2,2) */;

connection primary;

# from now on checking rbr specific idempotent errors
set @save_binlog_format= @@session.binlog_format;
set @@session.binlog_format= row;
delete from ti1 where b=1;

select * from ti1 order by b /* must be (2),(3) */;

# replica must catch up (expect some warnings in error.log)
--source include/sync_replica_sql_with_primary.inc

#connection replica;
select * from ti1 order by b /* must stays as were on primary (1),(2),(3) */;

delete from ti1 where b=3;

connection primary;
insert into ti2 set a=3, b=3;

# replica must catch up (expect some warnings in error.log)
--source include/sync_replica_sql_with_primary.inc

#connection replica;
select * from ti2 order by b /* must be (1,1),(2,2) - not inserted */;


#
# Checking the new global sys variable
#

connection replica;

set global replica_exec_mode='IDEMPOTENT';
set global replica_exec_mode='STRICT';

# checking mutual exclusion for the options
--error ER_WRONG_VALUE_FOR_VAR
set global replica_exec_mode='IDEMPOTENT,STRICT';

select @@global.replica_exec_mode /* must be STRICT */;

#
# Checking stops.
# In the following sections strict replica sql thread is going to
# stop when faces an idempotent error. In order to proceed
# the mode is temporarily switched to indempotent.
#

#
--echo *** foreign keys errors as above now forces to stop
#

connection primary;

set foreign_key_checks=0;
drop table ti2, ti1;

create table ti1 (b int primary key) engine = innodb;
create table ti2 (a int primary key, b int, foreign key (b) references ti1(b))
     engine = innodb;
set foreign_key_checks=1 /* ensure the check */;

insert into ti1 values (1),(2),(3);
insert into ti2 set a=2, b=2;

--source include/sync_replica_sql_with_primary.inc

#connection replica;
select * from ti1 order by b /* must be (1),(2),(3) */;
--echo *** conspire future problem
insert into ti2 set a=1, b=1;
select * from ti2 order by b /* must be (1,1) (2,2) */;

connection primary;

delete from ti1 where b=1 /* offending delete event */;
select * from ti1 order by b /* must be (2),(3) */;

# foreign key: row is referenced

--echo *** replica must stop (Trying to delete a referenced foreing key)
connection replica;
source include/wait_for_replica_sql_to_stop.inc;

let $last_error = query_get_value("SHOW REPLICA STATUS", Last_SQL_Errno, 1);
disable_query_log;
eval SELECT "$last_error" AS Last_SQL_Error;
enable_query_log;

select * from ti1 order by b /* must be (1),(2),(3) - not deleted */;
set foreign_key_checks= 0;
delete from ti2 where b=1;
set foreign_key_checks= 1;
set global replica_exec_mode='IDEMPOTENT';
start replica sql_thread;
connection primary;
--source include/sync_replica_sql_with_primary.inc
#connection replica;
set global replica_exec_mode='STRICT';

connection primary;

--source include/sync_replica_sql_with_primary.inc

#connection replica;
--echo *** conspire the following insert failure
# foreign key: no referenced row

--echo *** conspire future problem
delete from ti1 where b=3;

connection primary;
insert into ti2 set a=3, b=3 /* offending write event */;

--echo *** replica must stop (Trying to insert an invalid foreign key)
connection replica;
source include/wait_for_replica_sql_to_stop.inc;

let $last_error = query_get_value("SHOW REPLICA STATUS", Last_SQL_Errno, 1);
disable_query_log;
eval SELECT "$last_error" AS Last_SQL_Error;
enable_query_log;

select * from ti2 order by b /* must be (2,2) */;
set foreign_key_checks= 0;
insert into ti1 set b=3;
set foreign_key_checks= 1;
set global replica_exec_mode='IDEMPOTENT';
start replica sql_thread;
connection primary;
--source include/sync_replica_sql_with_primary.inc
#connection replica;
set global replica_exec_mode='STRICT';

connection primary;

--source include/sync_replica_sql_with_primary.inc

select * from ti2 order by b /* must be (2,2),(3,3) */;

# 
--echo *** other errors
# 

# dup key insert

#connection replica;
--echo *** conspiring query
insert into ti1 set b=1;

connection primary;
insert into ti1 set b=1 /* offending write event */;

--echo *** replica must stop (Trying to insert a dupliacte key)
connection replica;
source include/wait_for_replica_sql_to_stop.inc;

let $last_error = query_get_value("SHOW REPLICA STATUS", Last_SQL_Errno, 1);
disable_query_log;
eval SELECT "$last_error" AS Last_SQL_Error;
enable_query_log;

set foreign_key_checks= 0;
delete from ti1 where b=1;
set foreign_key_checks= 1;
set global replica_exec_mode='IDEMPOTENT';
start replica sql_thread;
connection primary;
--source include/sync_replica_sql_with_primary.inc
#connection replica;
set global replica_exec_mode='STRICT';

# key not found

connection primary;

CREATE TABLE t1 (a INT PRIMARY KEY);
CREATE TABLE t2 (a INT);
INSERT INTO t1 VALUES (-1),(-2),(-3);
INSERT INTO t2 VALUES (-1),(-2),(-3);
--source include/sync_replica_sql_with_primary.inc

#connection replica;
DELETE FROM t1 WHERE a = -2;
DELETE FROM t2 WHERE a = -2;
connection primary;
DELETE FROM t1 WHERE a = -2;

--echo *** replica must stop (Key was not found)
connection replica;
source include/wait_for_replica_sql_to_stop.inc;

let $last_error = query_get_value("SHOW REPLICA STATUS", Last_SQL_Errno, 1);
disable_query_log;
eval SELECT "$last_error" AS Last_SQL_Error;
enable_query_log;

set global replica_exec_mode='IDEMPOTENT';
start replica sql_thread;
connection primary;
--source include/sync_replica_sql_with_primary.inc
#connection replica;
set global replica_exec_mode='STRICT';

connection primary;
DELETE FROM t2 WHERE a = -2; 
--echo *** replica must stop (Key was not found)
connection replica;
source include/wait_for_replica_sql_to_stop.inc;

let $last_error = query_get_value("SHOW REPLICA STATUS", Last_SQL_Errno, 1);
disable_query_log;
eval SELECT "$last_error" AS Last_SQL_Error;
enable_query_log;

set global replica_exec_mode='IDEMPOTENT';
start replica sql_thread;
connection primary;
--source include/sync_replica_sql_with_primary.inc
#connection replica;
set global replica_exec_mode='STRICT';

UPDATE t1 SET a = 1 WHERE a = -1;
UPDATE t2 SET a = 1 WHERE a = -1;

connection primary;
UPDATE t1 SET a = 1 WHERE a = -1;

--echo *** replica must stop (Key was not found)
connection replica;
source include/wait_for_replica_sql_to_stop.inc;

let $last_error = query_get_value("SHOW REPLICA STATUS", Last_SQL_Errno, 1);
disable_query_log;
eval SELECT "$last_error" AS Last_SQL_Error;
enable_query_log;

set global replica_exec_mode='IDEMPOTENT';
start replica sql_thread;
connection primary;
--source include/sync_replica_sql_with_primary.inc
#connection replica;
set global replica_exec_mode='STRICT';


connection primary;
UPDATE t2 SET a = 1 WHERE a = -1;

--echo *** replica must stop (Key was not found)
connection replica;
source include/wait_for_replica_sql_to_stop.inc;

let $last_error = query_get_value("SHOW REPLICA STATUS", Last_SQL_Errno, 1);
disable_query_log;
eval SELECT "$last_error" AS Last_SQL_Error;
enable_query_log;

set global replica_exec_mode='IDEMPOTENT';
start replica sql_thread;
connection primary;
--source include/sync_replica_sql_with_primary.inc
#connection replica;
SET @@global.replica_exec_mode= @old_replica_exec_mode;

# cleanup for bug#31609 tests

connection primary;

drop table t1,t2,ti2,ti1;
--source include/sync_replica_sql_with_primary.inc
set @@global.replica_exec_mode= @old_replica_exec_mode;

