include/primary-replica.inc
Warnings:
Note	####	Sending passwords in plain text without SSL/TLS is extremely insecure.
Note	####	Storing MySQL user name or password information in the primary info repository is not secure and is therefore not recommended. Please consider using the USER and PASSWORD connection options for START REPLICA; see the 'START REPLICA Syntax' in the MySQL Manual for more information.
[connection primary]
[connection replica]
include/stop_replica.inc
#
# Explicitly simulate a NULL primary_uuid return value i.e., fake as a pre-5.6 primary.
#
SET @save_debug= @@GLOBAL.debug;
SET GLOBAL debug= '+d,dbug.return_null_PRIMARY_UUID';
#
# We should still have the primary_uuid value.
#
include/assert.inc [source_uuid field should be non-empty.]
#
# START REPLICA will return a NULL for primary_uuid now as per our debug behaviour.
#
include/start_replica.inc
#
# We dont have a primary_uuid now, so should see an empty output as the primary_uuid value.
#
#
# de-activate the debug behaviour.
#
SET GLOBAL debug= '-d,dbug.return_null_PRIMARY_UUID';
SET GLOBAL debug= @save_debug;
#
# After de-activating, the primary_uuid value should be there.
#
include/stop_replica.inc
include/start_replica.inc
#
# Verify that we have the primary_uuid now.
#
include/rpl_end.inc
